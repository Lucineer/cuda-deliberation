[package]
name = "cuda-deliberation"
version = "0.2.0"
edition = "2021"
description = "Consider/Resolve/Forfeit deliberation protocol — uses cuda-equipment"

[dependencies]
# cuda-equipment = { path = "../cuda-equipment" }
