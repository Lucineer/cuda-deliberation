[package]
name = "cuda-deliberation"
version = "0.1.0"
edition = "2021"
description = "Consider/Resolve/Forfeit protocol — multi-agent deliberation engine"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
